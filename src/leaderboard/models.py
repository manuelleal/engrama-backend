"""El módulo leaderboard NO tiene tablas propias.

Es de solo-lectura: arma el ranking a partir de tablas que ya existen en
`src/shared/models.py` (`Profile`, `Membership`, `CoinWallet`). Este archivo
existe solo para respetar la estructura de módulo obligatoria de
`.windsurfrules` (router.py / service.py / schemas.py / models.py).
"""
