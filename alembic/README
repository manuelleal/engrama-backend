Alembic migrations — engrama-backend

Config: alembic.ini (raíz del repo) + alembic/env.py (async con asyncpg).
La URL se lee de DATABASE_URL en .env.

Comandos útiles:
  poetry run alembic upgrade head
  poetry run alembic downgrade base
  poetry run alembic current
  poetry run alembic history
